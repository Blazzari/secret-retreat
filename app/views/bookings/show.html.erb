<div class="card rounded-4 p-3 container mt-5 mb-5">
  <div class="d-flex justify-content-between align-items-center ">
    <div class="mb-2">
      <h3>Your stay at "<%= @booking.room.title %>" is booked !</h2>
      <p class="mb-0">From : <%= @booking.start_date %> - To : <%= @booking.end_date %></p>
      <p class="mb-0">Duration : <%= @duration %> days - Price : <%= @price %> $</p>
    </div>
      <%= link_to "Back to rooms", rooms_path, class: "button" %>
  </div>
  <%= image_tag "love room 1.jpeg", alt: "Secret retreat",  style: 'height:800px; width:auto;', class: "rounded-4 mb-0" %>
</div>
